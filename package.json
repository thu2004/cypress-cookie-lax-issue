{
  "name": "cypress-cookie-lax-issue",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.1",
    "@testing-library/react": "^12.1.2",
    "@testing-library/user-event": "^13.5.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0",
    "web-vitals": "^2.1.3"
  },
  "scripts": {
    "start-app": "export HTTPS=true&&SSL_CRT_FILE=server.cert&&SSL_KEY_FILE=server.key react-scripts start",
    "start-cypress": "cypress open",
    "gen-cert": "openssl req -nodes -new -x509 -keyout server.key -out server.cert -config openssl.conf"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cypress": "^9.3.1"
  }
}
